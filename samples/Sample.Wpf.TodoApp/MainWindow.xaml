<Window x:Class="Sample.Wpf.TodoApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Sample.Wpf.TodoApp"
        mc:Ignorable="d"
        Title="Marionette TodoApp"
        Height="560" Width="500"
        Background="{StaticResource BgBrush}"
        WindowStartupLocation="CenterScreen"
        d:DataContext="{d:DesignInstance Type=local:TodoListViewModel}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Marionette TodoApp"
                       FontSize="22"
                       FontWeight="SemiBold"
                       Foreground="{StaticResource TextBrush}" />
            <TextBlock Text="An MCP-controllable WPF reference sample. Try it: claude mcp add it, then ask Claude to add a todo."
                       FontSize="11"
                       Foreground="{StaticResource MutedBrush}"
                       TextWrapping="Wrap"
                       Margin="0,4,0,0" />
        </StackPanel>

        <!-- Counts strip -->
        <Border Grid.Row="1"
                Background="{StaticResource PanelBrush}"
                BorderBrush="{StaticResource BorderBrush2}"
                BorderThickness="1"
                CornerRadius="6"
                Padding="14,10"
                Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="TOTAL"
                               FontSize="10"
                               Foreground="{StaticResource MutedBrush}" />
                    <TextBlock Text="{Binding TotalCount, FallbackValue=0}"
                               FontSize="20"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource TextBrush}" />
                </StackPanel>
                <StackPanel Grid.Column="1">
                    <TextBlock Text="DONE"
                               FontSize="10"
                               Foreground="{StaticResource MutedBrush}" />
                    <TextBlock Text="{Binding CompletedCount, FallbackValue=0}"
                               FontSize="20"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource DoneBrush}" />
                </StackPanel>
                <StackPanel Grid.Column="2">
                    <TextBlock Text="PENDING"
                               FontSize="10"
                               Foreground="{StaticResource MutedBrush}" />
                    <TextBlock Text="{Binding PendingCount, FallbackValue=0}"
                               FontSize="20"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource AccentBrush}" />
                </StackPanel>
            </Grid>
        </Border>

        <!-- Add row -->
        <Grid Grid.Row="2" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="NewTodoTextBox"
                     Grid.Column="0"
                     Margin="0,0,8,0"
                     KeyDown="NewTodoTextBox_KeyDown" />
            <Button x:Name="AddButton"
                    Grid.Column="1"
                    Content="Add"
                    Style="{StaticResource AccentButton}"
                    Click="AddButton_Click" />
        </Grid>

        <!-- Items list -->
        <Border Grid.Row="3"
                Background="{StaticResource PanelBrush}"
                BorderBrush="{StaticResource BorderBrush2}"
                BorderThickness="1"
                CornerRadius="6">
            <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="0">
                <ItemsControl x:Name="TodoItemsControl"
                              ItemsSource="{Binding Items}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="{StaticResource BorderBrush2}"
                                    BorderThickness="0,0,0,1"
                                    Padding="12,8">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <CheckBox Grid.Column="0"
                                              IsChecked="{Binding IsDone, Mode=TwoWay}"
                                              VerticalAlignment="Center"
                                              Margin="0,0,10,0" />
                                    <TextBlock Grid.Column="1"
                                               Text="{Binding Title}"
                                               VerticalAlignment="Center"
                                               TextTrimming="CharacterEllipsis">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsDone}" Value="True">
                                                        <Setter Property="TextDecorations" Value="Strikethrough" />
                                                        <Setter Property="Foreground" Value="{StaticResource MutedBrush}" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                    <Button Grid.Column="2"
                                            Content="Remove"
                                            Style="{StaticResource DangerButton}"
                                            Tag="{Binding}"
                                            Click="RemoveItemButton_Click" />
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <!-- Footer actions -->
        <Grid Grid.Row="4" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0"
                       VerticalAlignment="Center"
                       FontSize="11"
                       Foreground="{StaticResource MutedBrush}"
                       Text="{Binding LastAddedTitle, Mode=OneWay, StringFormat='Last added: {0}', FallbackValue='Last added: (none)', TargetNullValue='Last added: (none)'}" />
            <Button x:Name="ClearCompletedButton"
                    Grid.Column="1"
                    Content="Clear completed"
                    Style="{StaticResource GhostButton}"
                    Click="ClearCompletedButton_Click" />
        </Grid>

        <!-- Bottom hint -->
        <TextBlock Grid.Row="5"
                   Margin="0,16,0,0"
                   FontSize="10"
                   Foreground="{StaticResource MutedBrush}"
                   TextWrapping="Wrap"
                   HorizontalAlignment="Center">
            <Run Text="Marionette root: TodoListViewModel" />
            <Run Text="  ·  " />
            <Run Text="5 callables, 4 observables (3 watchable)" />
        </TextBlock>
    </Grid>
</Window>
